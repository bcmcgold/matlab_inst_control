function set_motor( inst_obj , set_mode , set_value )

    switch set_mode
        case {'Angle'}
            writeline(inst_obj,num2str(set_value)); % write angle setting to serial por
        otherwise
            warning('No matching mode');
    end

end

